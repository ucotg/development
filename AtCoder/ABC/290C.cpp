#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    int n, k;
    cin >> n >> k;
    int a[n + 1];

    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
    }
}